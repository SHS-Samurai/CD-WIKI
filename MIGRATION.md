# Datenmigration

Es liegen keine produktiven Django-/MySQL-Bestandsdaten für dieses Wiki vor. Daher ist kein Importlauf erforderlich; der Code unter `DEPRECATED/` bleibt unverändert als Funktionsreferenz erhalten.
