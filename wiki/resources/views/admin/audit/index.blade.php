<x-wiki-layout title="Auditlog">
    <div class="mb-8"><h1 class="text-3xl font-bold">Auditlog</h1><p class="mt-2 text-slate-600">Unveränderliches Protokoll sicherheitsrelevanter Aktionen.</p></div>
    <form class="mb-5 flex gap-3" method="get"><select class="rounded-md border-slate-300" name="action"><option value="">Alle Aktionen</option>@foreach ($actions as $item)<option value="{{ $item }}" @selected($action === $item)>{{ $item }}</option>@endforeach</select><button class="rounded-md bg-sky-700 px-4 py-2 text-white" type="submit">Filtern</button></form>
    <div class="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-100"><tr><th class="px-4 py-3">Zeit</th><th class="px-4 py-3">Aktion</th><th class="px-4 py-3">Benutzer</th><th class="px-4 py-3">Ziel</th></tr></thead><tbody class="divide-y divide-slate-200">@foreach ($logs as $log)<tr><td class="px-4 py-3"><a class="text-sky-700" href="{{ route('admin.audit.show', $log) }}">{{ $log->created_at->format('d.m.Y H:i:s') }}</a></td><td class="px-4 py-3">{{ $log->action }}</td><td class="px-4 py-3">{{ $log->user_name ?: 'System' }}</td><td class="px-4 py-3">{{ class_basename($log->target_type ?: '') }} {{ $log->target_id }}</td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $logs->links() }}</div>
</x-wiki-layout>
