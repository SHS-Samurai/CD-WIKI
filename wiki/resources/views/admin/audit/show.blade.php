<x-wiki-layout :title="'Auditlog #'.$log->id"><a class="text-sm font-semibold text-sky-700" href="{{ route('admin.audit.index') }}">← Auditlog</a><h1 class="mb-8 mt-2 text-3xl font-bold">Auditlog #{{ $log->id }}</h1><dl class="grid gap-4 rounded-xl border border-slate-200 bg-white p-6 sm:grid-cols-[12rem_1fr]">@foreach (['created_at' => 'Zeitpunkt', 'action' => 'Aktion', 'user_name' => 'Benutzer', 'ip_address' => 'IP-Adresse', 'user_agent' => 'User-Agent', 'target_type' => 'Zieltyp', 'target_id' => 'Ziel-ID', 'old_revision' => 'Alte Revision', 'new_revision' => 'Neue Revision'] as $field => $label)<dt class="font-semibold">{{ $label }}</dt><dd class="break-all">{{ $field === 'created_at' ? $log->created_at->format('d.m.Y H:i:s') : ($log->{$field} ?? '–') }}</dd>@endforeach<dt class="font-semibold">Details</dt><dd><pre class="overflow-auto rounded bg-slate-900 p-4 text-sm text-white">{{ json_encode($log->details, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES) }}</pre></dd></dl></x-wiki-layout>
