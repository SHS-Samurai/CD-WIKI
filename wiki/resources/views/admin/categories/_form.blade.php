<form class="space-y-6 rounded-xl border border-slate-200 bg-white p-6 shadow-sm" method="post" action="{{ $action }}">
    @csrf @if ($method !== 'POST') @method($method) @endif
    <label class="block"><span class="text-sm font-semibold">Name</span><input class="mt-2 block w-full rounded-md border-slate-300" name="name" type="text" value="{{ old('name', $category->name) }}" required maxlength="120">@error('name')<span class="mt-1 block text-sm text-red-700">{{ $message }}</span>@enderror</label>
    <label class="block"><span class="text-sm font-semibold">URL-Kennung</span><input class="mt-2 block w-full rounded-md border-slate-300 font-mono" name="slug" type="text" value="{{ old('slug', $category->slug) }}" required maxlength="120" pattern="[A-Za-z0-9_-]+">@error('slug')<span class="mt-1 block text-sm text-red-700">{{ $message }}</span>@enderror</label>
    <label class="block"><span class="text-sm font-semibold">Beschreibung</span><textarea class="mt-2 block min-h-28 w-full rounded-md border-slate-300" name="description">{{ old('description', $category->description) }}</textarea>@error('description')<span class="mt-1 block text-sm text-red-700">{{ $message }}</span>@enderror</label>
    <div class="flex gap-3"><button class="rounded-md bg-sky-700 px-5 py-2.5 font-semibold text-white hover:bg-sky-800" type="submit">Speichern</button><a class="rounded-md px-4 py-2.5 text-slate-600 hover:bg-slate-100" href="{{ route('admin.categories.index') }}">Abbrechen</a></div>
</form>
