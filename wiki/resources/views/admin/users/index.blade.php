<x-wiki-layout title="Benutzerverwaltung"><div class="mb-8 flex items-center justify-between"><div><h1 class="text-3xl font-bold">Benutzer</h1><p class="mt-2 text-slate-600">Konten, Administratoren und Gruppen verwalten.</p></div><a class="rounded-md bg-sky-700 px-4 py-2 font-semibold text-white" href="{{ route('admin.users.create') }}">Benutzer anlegen</a></div><div class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">@foreach ($users as $user)<a class="flex items-center justify-between gap-4 border-b border-slate-200 px-6 py-4 last:border-0 hover:bg-sky-50" href="{{ route('admin.users.edit', $user) }}"><span><strong>{{ $user->name }}</strong><small class="ml-2 text-slate-500">{{ $user->email }}</small>@if(!$user->approved_at)<small class="ml-2 rounded bg-amber-100 px-2 py-1 text-amber-800">Freigabe ausstehend</small>@endif</span><span class="text-sm">{{ $user->is_admin ? 'Administrator' : $user->groups->pluck('name')->join(', ') }}</span></a>@endforeach</div><div class="mt-6">{{ $users->links() }}</div></x-wiki-layout>
